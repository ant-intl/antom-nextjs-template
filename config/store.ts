/**
 * Storefront identity. This is a demo template — change these to rebrand.
 */
export const STORE = {
  name: 'Demo Store',
  /** Shown in the footer / metadata. */
  tagline: 'Premium audio, shipped worldwide.',
} as const;
